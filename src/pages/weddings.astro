---
import { getImage, Image } from 'astro:assets';
import BaseLayout from '../layouts/BaseLayout.astro';
import Eyebrow from '../components/Eyebrow.astro';
import CtaBand from '../components/CtaBand.astro';
import { testimonials, weddingFaqs } from '../data/site';
import { fleet, fleetImages } from '../data/fleet';
import { withBase } from '../utils/paths';
import weddingBannerBackground from '../assets/images/wedding/wedding-banner-bg.png';
import trolleyCoachPackageImage from '../assets/images/wedding/trolley-coach.png';
import trolleyCoachPackageDesktopImage from '../assets/images/wedding/trolley-coach-desktop.png';

const classicTrolley = fleet.find((vehicle) => vehicle.slug === 'classic-white-trolley');
if (!classicTrolley) throw new Error('Classic White Trolley fleet data is required for the wedding page.');

const weddingTestimonials = [testimonials[0], testimonials[1], testimonials[5]];
const trolleyQuoteHref = withBase(
  `/?vehicle=${encodeURIComponent(classicTrolley.quoteValue)}#getquote`,
);
const trolleyHref = withBase('/fleet/classic-white-trolley');
const packageMobileWidths = [480, 720, 960, 1200];
const packageMobileSources = await Promise.all(
  packageMobileWidths.map((width) =>
    getImage({
      src: trolleyCoachPackageImage,
      width,
      format: 'webp',
      quality: 84,
    }),
  ),
);
const packageMobileSrcset = packageMobileSources
  .map((image, index) => `${image.src} ${packageMobileWidths[index]}w`)
  .join(', ');
---

<BaseLayout
  title="Chicago Wedding Trolley Rental | ChiTown Trolley"
  description="Book a Chicago wedding trolley rental for the ceremony, photos and reception. Classic white trolley service for 30–36 guests across Chicagoland."
  image={fleetImages.trolley.classicExterior}
  hideCta={true}
>
  <!-- Full-width service hero -->
  <section
    class="whero"
    style={`background-image: url('${weddingBannerBackground.src}')`}
  >
    <div class="whero__overlay"></div>
    <div class="whero__content">
      <p class="whero__kicker">Wedding Trolley Transportation</p>
      <h1 class="whero__title">Chicago Wedding Trolley Rental</h1>
      <p class="whero__lead">
        A classic white trolley for the ceremony, photos, reception and every stop in between.
      </p>
      <div class="whero__actions">
        <a href={trolleyQuoteHref} class="btn-gold">Check Wedding Availability</a>
        <a href={trolleyHref} class="btn-ghost">View the Wedding Trolley</a>
      </div>
    </div>
  </section>

  <!-- Trolley and coach package -->
  <section class="wpackage" aria-labelledby="wedding-package-title">
    <div class="wpackage__inner wrap wrap-1140">
      <header class="wpackage__heading">
        <Eyebrow label="Wedding Package" mb={18} />
        <h2 id="wedding-package-title">Best-Selling Wedding Package</h2>
        <p>
          Keep your wedding party and guests moving together with one thoughtfully coordinated
          trolley and coach package.
        </p>
      </header>

      <div class="wpackage__card">
        <div class="wpackage__media">
          <div class="wpackage__visual">
            <picture>
              <source
                media="(max-width: 950px)"
                srcset={packageMobileSrcset}
                sizes="calc(100vw - 40px)"
                type="image/webp"
              />
              <Image
                src={trolleyCoachPackageDesktopImage}
                alt="White trolley and white coach bus parked together at a wedding venue"
                widths={[480, 720, 960, 1122]}
                sizes="(max-width: 1200px) 56vw, 630px"
                format="webp"
                quality={84}
                loading="lazy"
                decoding="async"
                class="wpackage__image"
              />
            </picture>
            <span class="wpackage__badge">Most Popular</span>
          </div>
        </div>

        <div class="wpackage__copy">
          <p class="wpackage__kicker">Package at a glance</p>

          <div class="wpackage__vehicles">
            <div class="wpackage__vehicle">
              <span class="wpackage__vehicleMark" aria-hidden="true">T</span>
              <div class="wpackage__vehicleCopy">
                <h3>Classic White Trolley</h3>
                <p>Up to 30 passengers</p>
              </div>
              <p class="wpackage__duration"><strong>5</strong><span>hours</span></p>
            </div>

            <div class="wpackage__vehicle">
              <span class="wpackage__vehicleMark" aria-hidden="true">C</span>
              <div class="wpackage__vehicleCopy">
                <h3>Coach Bus</h3>
                <p>Up to 50 guests</p>
              </div>
              <p class="wpackage__duration"><strong>8</strong><span>hours</span></p>
            </div>
          </div>

          <div class="rule-gold wpackage__rule"></div>
          <h3 class="wpackage__includesTitle">Everything your day needs</h3>
          <ul class="wpackage__features">
            <li><strong>Guest shuttle timing</strong><span>Recommended service from 4:00 p.m. to midnight</span></li>
            <li><strong>Flexible itinerary</strong><span>Unlimited scheduled stops and mileage</span></li>
            <li><strong>Comfort included</strong><span>Climate control, Bluetooth audio, comfortable seating and luggage storage</span></li>
            <li><strong>Professional service</strong><span>Experienced chauffeurs for both vehicles</span></li>
          </ul>

          <div class="wpackage__actions">
            <button
              type="button"
              class="btn-gold"
              data-package-modal-open
              aria-haspopup="dialog"
              aria-controls="wedding-package-dialog"
            >GET QUOTE</button>
            <p>Share your date and itinerary—we’ll confirm the details.</p>
          </div>
        </div>
      </div>
    </div>
  </section>

  <dialog
    class="packageModal"
    id="wedding-package-dialog"
    data-package-modal
    aria-labelledby="wedding-package-dialog-title"
  >
    <div class="packageModal__panel">
      <button
        type="button"
        class="packageModal__close"
        data-package-modal-close
        aria-label="Close wedding package availability form"
      >×</button>
      <p class="packageModal__kicker">Best-Selling Wedding Package</p>
      <h2 id="wedding-package-dialog-title">Check Wedding Package Availability</h2>
      <p class="packageModal__intro">
        Share your wedding details and we’ll confirm availability for the trolley and coach package.
      </p>

      <form
        class="packageForm"
        name="quote-request"
        method="POST"
        data-netlify="true"
        data-netlify-form="true"
        data-netlify-honeypot="bot-field"
        data-package-form
      >
        <input type="hidden" name="form-name" value="quote-request" />
        <input type="hidden" name="notification-form" value="wedding-quote-request" />
        <input type="hidden" name="type" value="Wedding" />
        <input type="hidden" name="hours" value="Trolley: 5 hours; Coach Bus: 8 hours" />
        <p class="netlify-honeypot" aria-hidden="true">
          <label>
            Do not fill this out:
            <input name="bot-field" tabindex="-1" autocomplete="off" />
          </label>
        </p>

        <label class="field">
          <span class="field__label">Full Name</span>
          <input required name="name" type="text" autocomplete="name" class="field__control" />
        </label>
        <label class="field">
          <span class="field__label">Email Address</span>
          <input required name="email" type="email" autocomplete="email" class="field__control" />
        </label>
        <label class="field">
          <span class="field__label">Phone Number</span>
          <input required name="phone" type="tel" autocomplete="tel" class="field__control" />
        </label>
        <label class="field">
          <span class="field__label">Wedding Date</span>
          <input required name="date" type="date" class="field__control" />
        </label>
        <label class="field">
          <span class="field__label">Estimated Guest Count</span>
          <input required name="passengers" type="number" min="1" inputmode="numeric" class="field__control" />
        </label>
        <label class="field">
          <span class="field__label">Preferred Trolley Pickup Time</span>
          <input name="time" type="time" class="field__control" />
        </label>
        <label class="field">
          <span class="field__label">Pickup Location</span>
          <input name="pickup" type="text" autocomplete="street-address" class="field__control" />
        </label>
        <label class="field">
          <span class="field__label">Reception or Final Destination</span>
          <input name="dropoff" type="text" class="field__control" />
        </label>
        <label class="field packageForm__full">
          <span class="field__label">Additional Details</span>
          <textarea
            name="message"
            rows="3"
            placeholder="Hotels, venues, shuttle timing or other stops"
            class="field__control"
          ></textarea>
        </label>
        <div class="packageForm__actions">
          <button type="submit" class="btn-gold">GET QUOTE</button>
        </div>
        <p class="form-status" data-form-status role="status" aria-live="polite"></p>
      </form>
    </div>
  </dialog>

  <!-- Wedding-day route -->
  <section class="wroute" aria-labelledby="wedding-routes-title">
    <div class="wrap wrap-1140">
      <Eyebrow label="Your Wedding Day" align="left" single mb={16} />
      <h2 id="wedding-routes-title" class="wroute__title">Wedding day transportation</h2>
      <p class="wroute__summary">
        Two simple routes keep the bridal party moving and wedding guests on schedule.
      </p>

      <div class="wroute__plans">
        <article class="wroute__plan">
          <header class="wroute__intro">
            <p class="wroute__vehicle">Classic White Trolley</p>
            <h3>Trolley for the Bridal Party</h3>
            <p>
              Keep the bridal party together from the residence and ceremony to photo locations
              and the reception. Share your timeline and planned stops, and we will help coordinate
              a route that keeps the day moving.
            </p>
          </header>

          <ol class="wroute__timeline wroute__timeline--trolley" aria-label="Bridal party trolley route">
            <li>
              <span class="wroute__marker" aria-hidden="true">1</span>
              <h4>Bridal Party Residence Pickup</h4>
              <p>Begin with one coordinated pickup for the couple, wedding party or immediate family.</p>
            </li>
            <li>
              <span class="wroute__marker" aria-hidden="true">2</span>
              <h4>Ceremony</h4>
              <p>Arrive together and keep the group on one schedule before the ceremony begins.</p>
            </li>
            <li>
              <span class="wroute__marker" aria-hidden="true">3</span>
              <h4>Photo Stops</h4>
              <p>Plan time for portraits at approved locations between the ceremony and reception.</p>
            </li>
            <li>
              <span class="wroute__marker" aria-hidden="true">4</span>
              <h4>Wedding Venue</h4>
              <p>Finish at the reception venue or continue with scheduled return transportation.</p>
            </li>
          </ol>
        </article>

        <article class="wroute__plan wroute__plan--coach">
          <header class="wroute__intro">
            <p class="wroute__vehicle">Super Coach Bus</p>
            <h3>Coach Bus for Wedding Guests</h3>
            <p>
              Keep wedding guests together from their hotels to the wedding venue and back. Share
              your timeline and hotel addresses, and we will help coordinate a route that keeps the
              day moving.
            </p>
          </header>

          <ol class="wroute__timeline wroute__timeline--coach" aria-label="Wedding guest coach bus route">
            <li>
              <span class="wroute__marker" aria-hidden="true">1</span>
              <h4>Hotel Pickup</h4>
              <p>Pick up wedding guests from one or multiple hotels for the trip to the wedding venue.</p>
            </li>
            <li>
              <span class="wroute__marker" aria-hidden="true">2</span>
              <h4>Wedding Venue Drop-Off</h4>
              <p>Arrive on time and keep guests on one schedule before the ceremony begins.</p>
            </li>
            <li class="wroute__wait">
              <span class="wroute__marker" aria-hidden="true">3</span>
              <span class="wroute__status">Waits on-site</span>
              <h4>Coach Waits at the Wedding Venue</h4>
              <p>
                After guests are dropped off, the coach parks at the wedding venue and waits there
                until return shuttle service begins.
              </p>
            </li>
            <li>
              <span class="wroute__marker" aria-hidden="true">4</span>
              <h4>Wedding Venue Pickup</h4>
              <p>Return shuttle service begins by picking up guests at the wedding venue.</p>
            </li>
            <li>
              <span class="wroute__marker" aria-hidden="true">5</span>
              <h4>Hotel Drop-Off</h4>
              <p>Guests are safely transported back to their hotels.</p>
            </li>
          </ol>
        </article>
      </div>
    </div>
  </section>

  <!-- Wedding testimonials -->
  <section class="wreviews">
    <div class="wrap wrap-1000">
      <div class="wreviews__head">
        <Eyebrow label="Wedding Reviews" align="left" single mb={16} />
        <h2>What Chicago couples remember most</h2>
      </div>
      <div class="wreviews__grid">
        {weddingTestimonials.map((testimonial, index) => (
          <blockquote class:list={['wreview', { 'wreview--featured': index === 0 }]}>
            <span class="wreview__mark" aria-hidden="true">“</span>
            <p>{testimonial.quote}</p>
            <footer>{testimonial.name}</footer>
          </blockquote>
        ))}
      </div>
    </div>
  </section>

  <!-- Wedding FAQs -->
  <section class="wfaq" aria-labelledby="wedding-faq-title">
    <div class="wrap wrap-860">
      <Eyebrow label="Wedding Trolley Questions" mb={16} />
      <h2 id="wedding-faq-title">Planning your wedding trolley rental</h2>
      <div class="wfaq__list" data-wedding-faq>
        {weddingFaqs.map((item, index) => {
          const buttonId = `wedding-faq-button-${index + 1}`;
          const panelId = `wedding-faq-panel-${index + 1}`;
          return (
            <div class="wfaq__item">
              <button
                id={buttonId}
                type="button"
                class="wfaq__head"
                aria-expanded="false"
                aria-controls={panelId}
                data-wedding-faq-toggle
              >
                <span>{item.q}</span>
                <span class="wfaq__chevron" aria-hidden="true"></span>
              </button>
              <div
                id={panelId}
                class="wfaq__panel"
                role="region"
                aria-labelledby={buttonId}
              >
                <div class="wfaq__body" inert>
                  <p>{item.a}</p>
                </div>
              </div>
            </div>
          );
        })}
      </div>
    </div>
  </section>

  <!-- Wedding quote CTA -->
  <div class="wedding-final">
    <CtaBand
      title="Reserve your Chicago wedding trolley"
      sub="Share your date, guest count and itinerary to check availability for your wedding day."
      ctaLabel="Wedding Trolley Quote"
      ctaHref={trolleyQuoteHref}
    />
  </div>
</BaseLayout>

<style>
  :where(.whero, .wroute, .wpackage, .wreviews, .wfaq) h2 {
    margin: 0;
    font-family: var(--serif);
    font-size: clamp(30px, 4vw, 48px);
    font-weight: 500;
    line-height: 1.08;
    color: var(--ink);
  }

  /* Hero */
  .whero {
    position: relative;
    height: 315px;
    overflow: hidden;
    background-color: #17140f;
    background-repeat: no-repeat;
    background-position: center;
    background-size: cover;
  }
  .whero__overlay {
    position: absolute;
    inset: 0;
    background:
      linear-gradient(
        90deg,
        rgba(12, 10, 7, .94) 0%,
        rgba(12, 10, 7, .82) 36%,
        rgba(12, 10, 7, .48) 62%,
        rgba(12, 10, 7, .1) 100%
      ),
      linear-gradient(0deg, rgba(12, 10, 7, .2), transparent 65%);
  }
  .whero__content {
    position: relative;
    z-index: 1;
    width: 100%;
    max-width: 1400px;
    height: 100%;
    display: flex;
    flex-direction: column;
    justify-content: center;
    align-items: flex-start;
    gap: 15px;
    margin: 0 auto;
    padding: clamp(24px, 4vw, 30px) clamp(24px, 6vw, 96px) 0;
    animation: fadeInUp 1.1s cubic-bezier(.22, .6, .2, 1) both;
  }
  .whero__kicker {
    margin: 0;
    color: var(--gold-light);
    font-size: 11px;
    line-height: 1.3;
    letter-spacing: .34em;
    text-transform: uppercase;
  }
  .whero__title {
    margin: 0;
    color: #fff;
    font-family: var(--serif);
    font-size: clamp(36px, 5.4vw, 70px);
    font-weight: 500;
    line-height: 1.1;
    text-shadow: 0 2px 24px rgba(0, 0, 0, .42);
  }
  .whero__lead {
    max-width: 46ch;
    margin: 0;
    color: rgba(253, 252, 250, .9);
    font-size: 16.5px;
    font-weight: 300;
    line-height: 1.7;
    letter-spacing: .02em;
    text-shadow: 0 2px 16px rgba(0, 0, 0, .35);
  }
  .whero__actions {
    display: flex;
    flex-wrap: wrap;
    gap: 12px;
    margin-top: 2px;
  }
  .whero__actions a {
    min-height: 44px;
    display: inline-flex;
    align-items: center;
    justify-content: center;
  }
  .whero__actions .btn-ghost {
    border-color: rgba(212, 175, 106, .78);
    background: rgba(10, 8, 6, .2);
    color: var(--paper);
  }
  .whero__actions .btn-ghost:hover {
    border-color: var(--gold-light);
    background: rgba(253, 252, 250, .1);
    color: #fff;
  }

  /* Route */
  .wroute { padding: 94px 28px 28px; background: var(--paper); }
  .wroute__title { max-width: 760px; }
  .wroute__summary {
    max-width: 610px;
    margin: 18px 0 0;
    color: var(--muted);
    font-size: 15.5px;
    font-weight: 300;
    line-height: 1.7;
  }
  .wroute__plans {
    display: grid;
    grid-template-columns: repeat(2, minmax(0, 1fr));
    gap: clamp(48px, 7vw, 82px);
    margin-top: 54px;
  }
  .wroute__plan {
    min-width: 0;
    padding-top: 27px;
    border-top: 1px solid var(--gold-dark);
  }
  .wroute__intro { max-width: 520px; }
  .wroute__vehicle {
    margin: 0 0 10px;
    color: var(--gold-deep);
    font-size: 9px;
    font-weight: 600;
    letter-spacing: .25em;
    line-height: 1.4;
    text-transform: uppercase;
  }
  .wroute__intro h3 {
    margin: 0;
    font-family: var(--serif);
    font-size: clamp(26px, 2.5vw, 34px);
    font-weight: 500;
    line-height: 1.18;
    color: var(--ink);
  }
  .wroute__intro > p:last-child {
    margin: 15px 0 0;
    color: var(--muted);
    font-size: 14.5px;
    font-weight: 300;
    line-height: 1.72;
  }
  .wroute__timeline {
    position: relative;
    margin: 30px 0 0;
    padding: 0;
    list-style: none;
  }
  .wroute__timeline::before {
    content: '';
    position: absolute;
    top: 31px;
    bottom: 32px;
    left: 14px;
    width: 1px;
    background: linear-gradient(180deg, var(--gold-dark), var(--line-2));
  }
  .wroute__timeline li {
    position: relative;
    display: grid;
    grid-template-columns: 29px minmax(0, 1fr);
    column-gap: 15px;
    padding: 17px 0 18px;
    border-top: 1px solid var(--line);
  }
  .wroute__marker {
    position: relative;
    z-index: 1;
    grid-column: 1;
    grid-row: 1 / span 3;
    width: 29px;
    height: 29px;
    display: grid;
    place-items: center;
    border: 1px solid var(--gold-dark);
    border-radius: 50%;
    background: var(--paper);
    color: var(--gold-deep);
    font-family: var(--serif);
    font-size: 12px;
    line-height: 1;
  }
  .wroute__timeline li:last-child .wroute__marker {
    background: var(--gold-deep);
    color: #fff;
  }
  .wroute__timeline li:last-child::after {
    content: '';
    position: absolute;
    top: 31px;
    bottom: 0;
    left: 13px;
    width: 3px;
    background: var(--paper);
  }
  .wroute__timeline h4 {
    grid-column: 2;
    margin: 0 0 6px;
    font-family: var(--serif);
    font-size: 18px;
    font-weight: 600;
    line-height: 1.3;
  }
  .wroute__timeline p {
    grid-column: 2;
    margin: 0;
    color: var(--muted);
    font-size: 13px;
    font-weight: 300;
    line-height: 1.65;
  }
  .wroute__timeline .wroute__wait {
    margin: 6px -16px;
    padding: 18px 16px 19px;
    border-top-color: transparent;
    border-left: 2px solid var(--gold-dark);
    background: rgba(212, 175, 106, .08);
  }
  .wroute__wait .wroute__marker {
    border-style: dashed;
    background: #f8f0e2;
    box-shadow: 0 0 0 4px var(--paper);
  }
  .wroute__status {
    grid-column: 2;
    margin: 0 0 6px;
    color: var(--gold-deep);
    font-size: 8px;
    font-weight: 700;
    letter-spacing: .2em;
    line-height: 1.4;
    text-transform: uppercase;
  }

  /* Trolley + coach package */
  .wpackage {
    padding: 32px 28px;
    background:
      radial-gradient(circle at 12% 18%, rgba(212, 175, 106, .09), transparent 28%),
      var(--cream);
  }
  .wpackage__heading {
    max-width: 720px;
    margin: 0 auto clamp(38px, 5vw, 56px);
    text-align: center;
  }
  .wpackage__heading > p {
    max-width: 620px;
    margin: 18px auto 0;
    color: var(--muted);
    font-size: 16px;
    font-weight: 300;
    line-height: 1.7;
  }
  .wpackage__card {
    display: grid;
    grid-template-columns: minmax(0, 1.08fr) minmax(390px, .92fr);
    align-items: stretch;
    overflow: hidden;
    border: 1px solid var(--line-2);
    border-radius: 14px;
    background: var(--paper);
    box-shadow: 0 24px 60px rgba(47, 36, 20, .1);
  }
  .wpackage__media {
    display: flex;
    min-width: 0;
    margin: 0;
    flex-direction: column;
    justify-content: center;
    padding: clamp(22px, 3vw, 36px);
    border-right: 1px solid var(--line-2);
    background: #f1eadf;
  }
  .wpackage__visual {
    position: relative;
    overflow: hidden;
    border-radius: 9px;
    box-shadow: 0 16px 34px rgba(35, 28, 17, .13);
  }
  .wpackage__visual :global(picture) { display: block; }
  .wpackage__visual :global(img) {
    display: block;
    width: 100%;
    height: auto;
  }
  .wpackage__badge {
    position: absolute;
    top: 16px;
    left: 16px;
    padding: 8px 12px 7px;
    border-radius: 999px;
    background: rgba(253, 252, 250, .94);
    color: var(--gold-deep);
    font-size: 9px;
    font-weight: 600;
    letter-spacing: .2em;
    line-height: 1;
    text-transform: uppercase;
    box-shadow: 0 5px 16px rgba(26, 26, 26, .12);
  }
  .wpackage__copy {
    display: flex;
    min-width: 0;
    flex-direction: column;
    justify-content: center;
    padding: clamp(34px, 4vw, 52px);
  }
  .wpackage__kicker {
    margin: 0 0 18px;
    color: var(--gold-deep);
    font-size: 10px;
    font-weight: 600;
    letter-spacing: .24em;
    line-height: 1.3;
    text-transform: uppercase;
  }
  .wpackage__vehicles { display: grid; gap: 10px; }
  .wpackage__vehicle {
    display: grid;
    grid-template-columns: auto minmax(0, 1fr) auto;
    align-items: center;
    gap: 14px;
    padding: 15px 16px;
    border: 1px solid var(--line-2);
    border-radius: 8px;
    background: var(--cream);
  }
  .wpackage__vehicleMark {
    display: grid;
    width: 36px;
    height: 36px;
    place-items: center;
    border: 1px solid var(--gold);
    border-radius: 50%;
    color: var(--gold-deep);
    font-family: var(--serif);
    font-size: 17px;
  }
  .wpackage__vehicleCopy h3 {
    margin: 0 0 3px;
    color: var(--ink);
    font-family: var(--serif);
    font-size: 19px;
    font-weight: 600;
    line-height: 1.15;
  }
  .wpackage__vehicleCopy p {
    margin: 0;
    color: var(--muted);
    font-size: 12.5px;
    font-weight: 300;
  }
  .wpackage__duration {
    display: flex;
    align-items: baseline;
    gap: 5px;
    margin: 0;
    color: var(--gold-deep);
  }
  .wpackage__duration strong {
    font-family: var(--serif);
    font-size: 28px;
    font-weight: 500;
    line-height: 1;
  }
  .wpackage__duration span {
    font-size: 9px;
    font-weight: 600;
    letter-spacing: .12em;
    text-transform: uppercase;
  }
  .wpackage__rule { width: 64px; margin: 26px 0 20px; }
  .wpackage__includesTitle {
    margin: 0 0 16px;
    color: var(--ink);
    font-family: var(--serif);
    font-size: 23px;
    font-weight: 500;
    line-height: 1.2;
  }
  .wpackage__features {
    display: grid;
    gap: 12px;
    margin: 0;
    padding: 0;
    list-style: none;
  }
  .wpackage__features li {
    position: relative;
    display: grid;
    gap: 2px;
    padding-left: 20px;
    color: var(--muted);
    font-size: 13px;
    font-weight: 300;
    line-height: 1.4;
  }
  .wpackage__features li::before {
    position: absolute;
    top: 6px;
    left: 2px;
    width: 6px;
    height: 6px;
    transform: rotate(45deg);
    background: var(--gold-dark);
    content: '';
  }
  .wpackage__features strong {
    color: var(--ink);
    font-size: 13px;
    font-weight: 500;
  }
  .wpackage__features span { display: block; }
  .wpackage__actions {
    display: flex;
    align-items: center;
    gap: 18px;
    margin-top: 28px;
  }
  .wpackage__actions .btn-gold {
    flex: none;
    min-width: 205px;
    text-align: center;
  }
  .wpackage__actions p {
    max-width: 190px;
    margin: 0;
    color: var(--gold-dark);
    font-size: 11px;
    font-weight: 300;
    line-height: 1.45;
  }

  /* Wedding package availability dialog */
  .packageModal {
    width: min(760px, calc(100% - 32px));
    max-height: calc(100dvh - 32px);
    margin: auto;
    padding: 0;
    overflow: auto;
    border: 1px solid var(--line-2);
    border-radius: 10px;
    color: var(--ink);
    background: var(--paper);
    box-shadow: 0 24px 80px rgba(16, 13, 9, .28);
  }
  .packageModal::backdrop {
    background: rgba(14, 12, 9, .72);
    backdrop-filter: blur(3px);
  }
  .packageModal__panel {
    position: relative;
    padding: 42px 46px 46px;
  }
  .packageModal__close {
    position: absolute;
    top: 16px;
    right: 18px;
    display: grid;
    width: 38px;
    height: 38px;
    cursor: pointer;
    place-items: center;
    padding: 0;
    border: 0;
    border-radius: 50%;
    color: var(--ink);
    background: transparent;
    font-family: var(--sans);
    font-size: 30px;
    font-weight: 300;
    line-height: 1;
    transition: color .2s ease, background .2s ease;
  }
  .packageModal__close:hover {
    color: var(--gold-dark);
    background: var(--cream);
  }
  .packageModal__kicker {
    margin: 0 48px 8px 0;
    color: var(--gold-deep);
    font-size: 10px;
    font-weight: 500;
    letter-spacing: .24em;
    text-transform: uppercase;
  }
  .packageModal h2 {
    margin: 0;
    padding-right: 40px;
    font-family: var(--serif);
    font-size: clamp(30px, 5vw, 42px);
    font-weight: 500;
    line-height: 1.08;
  }
  .packageModal__intro {
    max-width: 58ch;
    margin: 14px 0 30px;
    color: var(--muted);
    font-size: 14.5px;
    font-weight: 300;
    line-height: 1.65;
  }
  .packageForm {
    display: grid;
    grid-template-columns: 1fr 1fr;
    gap: 21px 28px;
  }
  .packageForm .field__control { width: 100%; }
  .packageForm__full,
  .packageForm__actions { grid-column: 1 / -1; }
  .packageForm__actions { margin-top: 4px; }
  :global(html.package-modal-open) { overflow: hidden; }

  @keyframes package-modal-in {
    from { opacity: 0; transform: translateY(10px) scale(.985); }
    to { opacity: 1; transform: translateY(0) scale(1); }
  }
  .packageModal[open] { animation: package-modal-in .22s ease both; }

  /* Testimonials */
  .wreviews { padding: 94px 28px; background: var(--paper); }
  .wreviews__head { max-width: 680px; margin-bottom: 46px; }
  .wreviews__grid {
    display: grid;
    grid-template-columns: 1.25fr .875fr .875fr;
    gap: 18px;
  }
  .wreview {
    position: relative;
    min-width: 0;
    margin: 0;
    padding: 34px 30px 30px;
    border: 1px solid var(--line);
    background: var(--cream);
  }
  .wreview--featured { background: var(--ink); border-color: var(--ink); }
  .wreview__mark {
    display: block;
    height: 30px;
    color: var(--gold);
    font-family: var(--serif);
    font-size: 52px;
    line-height: .8;
  }
  .wreview p {
    margin: 16px 0 26px;
    color: var(--ink);
    font-family: var(--serif);
    font-size: 18px;
    font-style: italic;
    line-height: 1.55;
  }
  .wreview--featured p { color: var(--paper); font-size: 21px; }
  .wreview footer {
    color: var(--muted);
    font-size: 10.5px;
    line-height: 1.5;
    letter-spacing: .16em;
    text-transform: uppercase;
  }
  .wreview--featured footer { color: rgba(253, 252, 250, .66); }

  /* FAQs */
  .wfaq { padding: 94px 28px 100px; background: var(--cream); }
  .wfaq > .wrap > h2 { margin-bottom: 38px; text-align: center; }
  .wfaq__list { margin: 0 auto; }
  .wfaq__item { border-top: 1px solid var(--line-2); }
  .wfaq__item:last-child { border-bottom: 1px solid var(--line-2); }
  .wfaq__head {
    width: 100%;
    min-height: 68px;
    display: flex;
    align-items: center;
    gap: 20px;
    padding: 22px 4px;
    border: 0;
    background: transparent;
    color: var(--ink);
    cursor: pointer;
    font-family: var(--serif);
    font-size: 19px;
    font-weight: 600;
    line-height: 1.35;
    text-align: left;
  }
  .wfaq__head span:first-child { flex: 1; }
  .wfaq__head:focus-visible { outline: 2px solid var(--gold-deep); outline-offset: 4px; }
  .wfaq__chevron {
    flex: none;
    width: 10px;
    height: 10px;
    border-right: 1.5px solid var(--gold-deep);
    border-bottom: 1.5px solid var(--gold-deep);
    transform: translateY(-2px) rotate(45deg);
    transition: transform .3s ease;
  }
  .wfaq__item.is-open .wfaq__chevron { transform: translateY(2px) rotate(-135deg); }
  .wfaq__panel {
    display: grid;
    grid-template-rows: 0fr;
    transition: grid-template-rows .35s ease;
  }
  .wfaq__item.is-open .wfaq__panel { grid-template-rows: 1fr; }
  .wfaq__body { min-height: 0; overflow: hidden; }
  .wfaq__body p {
    max-width: 68ch;
    margin: 0;
    padding: 0 4px 26px;
    color: var(--muted);
    font-size: 15px;
    font-weight: 300;
    line-height: 1.8;
  }

  .wedding-final :global(.cta-band) {
    opacity: 1;
    transform: none;
  }

  @media (max-width: 1024px) {
    .wreviews__grid { grid-template-columns: 1fr 1fr; }
    .wreview--featured { grid-column: 1 / 3; }
  }

  @media (max-width: 950px) {
    .wpackage__card {
      grid-template-columns: 1fr;
    }
    .wpackage__visual :global(img) { aspect-ratio: 1672 / 941; }
    .wpackage__media { border-right: 0; border-bottom: 1px solid var(--line-2); }
    .wpackage__copy {
      width: 100%;
      max-width: 880px;
      margin-inline: auto;
      padding: 38px;
    }
  }

  @media (max-width: 820px) {
    .wroute, .wpackage, .wreviews, .wfaq { padding-inline: 24px; }
    .wroute__plans { grid-template-columns: 1fr; gap: 52px; }
    .wroute__intro { max-width: 650px; }
  }

  @media (max-width: 640px) {
    .whero {
      height: 280px;
      background-position: center;
      background-size: cover;
    }
    .whero__overlay {
      background: linear-gradient(
        90deg,
        rgba(10, 8, 6, .94) 0%,
        rgba(10, 8, 6, .8) 52%,
        rgba(10, 8, 6, .43) 100%
      );
    }
    .whero__content {
      gap: 10px;
      padding: 18px 18px 0;
    }
    .whero__kicker { font-size: 9px; letter-spacing: .25em; }
    .whero__title { font-size: clamp(28px, 9vw, 40px); line-height: 1.05; }
    .whero__lead {
      max-width: 40ch;
      font-size: 12.5px;
      line-height: 1.45;
    }
    .whero__actions { gap: 7px; margin-top: 0; }
    .whero__actions a {
      min-height: 44px;
      padding: 10px 12px;
      font-size: 9px;
      letter-spacing: .15em;
      white-space: nowrap;
    }
    .wroute, .wpackage, .wreviews, .wfaq {
      padding: 68px 20px;
    }
    .wroute { padding-bottom: 20px; }
    .wpackage { padding-block: 24px; }
    .wroute__plans { gap: 44px; margin-top: 38px; }
    .wroute__plan { padding-top: 22px; }
    .wroute__timeline { margin-top: 25px; }
    .wroute__timeline .wroute__wait { margin-inline: -10px; padding-inline: 10px; }
    .wpackage__heading { margin-bottom: 34px; }
    .wpackage__media { padding: 18px; }
    .wpackage__copy { padding: 32px 24px 34px; }
    .wpackage__actions { align-items: stretch; flex-direction: column; gap: 12px; }
    .wpackage__actions .btn-gold { width: 100%; min-height: 44px; padding: 13px 16px; }
    .wpackage__actions p { max-width: none; text-align: center; }
    .packageModal { width: min(100% - 20px, 760px); max-height: calc(100dvh - 20px); }
    .packageModal__panel { padding: 36px 24px 30px; }
    .packageModal__close { top: 9px; right: 10px; }
    .packageModal h2 { padding-right: 22px; }
    .packageModal__intro { margin-bottom: 25px; }
    .packageForm { grid-template-columns: 1fr; gap: 20px; }
    .packageForm__full,
    .packageForm__actions { grid-column: auto; }
    .packageForm__actions .btn-gold { width: 100%; min-height: 44px; padding: 13px 16px; }
    .wreviews__grid { grid-template-columns: 1fr; }
    .wreview--featured { grid-column: auto; }
    .wreview { padding: 30px 24px 26px; }
    .wfaq > .wrap > h2 { margin-bottom: 28px; }
    .wfaq__head { min-height: 60px; padding: 19px 2px; font-size: 17px; }
    .wfaq__body p { padding-inline: 2px; font-size: 14.5px; }
  }

  @media (max-width: 390px) {
    .whero__actions {
      flex-direction: column;
      align-items: flex-start;
      gap: 6px;
    }
    .wpackage__actions .btn-gold,
    .packageForm__actions .btn-gold { font-size: 10px; letter-spacing: .16em; }
    .wpackage__vehicle { grid-template-columns: auto minmax(0, 1fr); }
    .wpackage__duration { grid-column: 2; }
  }

  @media (prefers-reduced-motion: reduce) {
    .whero__content { animation: none; }
    .wfaq__panel, .wfaq__chevron { transition: none; }
    .packageModal[open] { animation: none; }
  }
</style>

<script>
  const packageModalOpen = document.querySelector<HTMLButtonElement>('[data-package-modal-open]');
  const packageModal = document.querySelector<HTMLDialogElement>('[data-package-modal]');
  const packageModalClose = packageModal?.querySelector<HTMLButtonElement>('[data-package-modal-close]');
  const packageForm = packageModal?.querySelector<HTMLFormElement>('[data-package-form]');

  packageModalOpen?.addEventListener('click', () => {
    const status = packageForm?.querySelector<HTMLElement>('[data-form-status]');
    if (status) {
      status.textContent = '';
      delete status.dataset.state;
    }
    packageModal?.showModal();
    document.documentElement.classList.add('package-modal-open');
  });

  packageModalClose?.addEventListener('click', () => packageModal?.close());

  packageModal?.addEventListener('click', (event) => {
    if (event.target === packageModal) packageModal.close();
  });

  packageModal?.addEventListener('close', () => {
    document.documentElement.classList.remove('package-modal-open');
  });

  const weddingFaq = document.querySelector<HTMLElement>('[data-wedding-faq]');
  if (weddingFaq) {
    const items = Array.from(weddingFaq.querySelectorAll<HTMLElement>('.wfaq__item'));
    const setOpen = (item: HTMLElement, open: boolean) => {
      item.classList.toggle('is-open', open);
      item
        .querySelector('[data-wedding-faq-toggle]')
        ?.setAttribute('aria-expanded', String(open));
      const body = item.querySelector<HTMLElement>('.wfaq__body');
      if (body) body.inert = !open;
    };

    items.forEach((item) => {
      item
        .querySelector<HTMLButtonElement>('[data-wedding-faq-toggle]')
        ?.addEventListener('click', () => {
          const wasOpen = item.classList.contains('is-open');
          items.forEach((otherItem) => setOpen(otherItem, false));
          if (!wasOpen) setOpen(item, true);
        });
      setOpen(item, false);
    });
  }
</script>
